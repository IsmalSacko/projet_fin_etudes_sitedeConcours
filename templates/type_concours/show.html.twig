{% extends 'base.html.twig' %}

{% block title %}TypeConcours{% endblock %}

{% block body %}
    <h1>TypeConcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_concour.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ type_concour.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_concours_index') }}">back to list</a>

    <a href="{{ path('type_concours_edit', {'id': type_concour.id}) }}">edit</a>

    {{ include('type_concours/_delete_form.html.twig') }}
{% endblock %}
